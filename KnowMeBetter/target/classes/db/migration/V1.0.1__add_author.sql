CREATE TABLE knowmebetter.author (
id SERIAL PRIMARY KEY,
optlock TIMESTAMP (3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
name VARCHAR(255)
);

CREATE TABLE knowmebetter.author2 (
id SERIAL PRIMARY KEY,
optlock TIMESTAMP (3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
name VARCHAR(255)
);

CREATE TABLE knowmebetter.author3 (
id SERIAL PRIMARY KEY,
optlock TIMESTAMP (3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
name VARCHAR(255)
);

CREATE TABLE knowmebetter.author4 (
id SERIAL PRIMARY KEY,
optlock TIMESTAMP (3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
name VARCHAR(255)
);

CREATE TABLE knowmebetter.author5 (
id SERIAL PRIMARY KEY,
optlock TIMESTAMP (3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
name VARCHAR(255)
);
